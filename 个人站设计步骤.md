## 第一步和智能体说：

根据我的简历，请帮我从零搭建一个个人作品集网站。

我的身份是视觉设计师/AI设计师/品牌设计师。网站整体要是暗色系，高级、克制、有科技感，但不要像模板网站。

页面需要包含:

1.全屏首页Hero，包含视频背景、大标题、导航栏和联系按钮

2.个人经历模块，包含头像或人物图、个人介绍、联系方式、项目数据3.精选项目模块，用大卡片展示作品图片

4.个人优势模块，用卡片展示我的能力

5.底部联系方式模块，做成整屏收尾页

页面需要适合PC端展示，版心控制在1700px左右。

### 给你的创作空间

- 你可以自由选择合适的颜色搭配（只要保持暗色系高级感）、字体系统和排版方式
- 你可以自由设计每个模块的具体布局和视觉表现形式
- 你可以加入符合我设计师身份的创意动效和交互细节
- 你可以在不影响核心功能的前提下，添加任何你认为能提升网站品质的元素

我相信你的审美和技术能力，期待看到一个有惊喜感的基础版本。

先完成一个可以运行和预览的基础版本，后续我会继续提供截图和参考网站让你优化。、

> **说明**：内容资产（简历文案、项目图片/视频、Hero视频）先用占位符，后期替换为真实素材。设计系统（配色、字体、间距、圆角等 token）由 AI 根据项目类型自行决定，无需手动定义。

&nbsp;

## 第二步

输入合适的参考图，修改风格

## 第三步

参考网站 https\://www\.reactbits.dev/animations/animated-content   【~复制提示】发给智能体，说明应用到那个地方。

## 第四步

添加进场动画。"我想要更像高端设计师作品集/创意机构官网的动效，不要普通淡入。首屏需要有完整opening animation，标题要有强视觉进场，比如遮罩揭开、位移、压缩后归位。滚动到每个模块时，英文大标题先大幅进场，卡片再依次stagger 出现，图片要有reveal 或轻微 parallax。整体动效要夸张一点但高级，节奏慢一些、缓动丝滑，不要廉价弹跳，也不要影响性能。可以用GSAP+ScrolITrigger。

## 第五步

##### 保证流畅加载请帮我审查性能，重点检查:

1.视频和图片体积

2.WebGL动态背景

3.滚动监听

4.GSAP动画

5.首屏加载如果有问题，请直接帮我优化。

&nbsp;

## 第六步 — 项目详情页 & 交互补全

把以下完整指令发给智能体：

---

现在基础版本已经完成，请帮我补全交互细节和项目详情页。具体要求：

### 6.1 项目详情页

点击精选项目卡片，打开全屏项目详情弹窗（modal），内容包括：
- 项目大图 / 视频（支持多图轮播）
- 项目标题 + 副标题
- 项目背景描述（2-3 段文案）
- 我的角色与职责（标签形式展示）
- 项目成果 / 数据亮点
- 上下项目导航（上一个 / 下一个）
- ESC 键或点击遮罩关闭
- 打开 / 关闭需要丝滑过渡动画

### 6.2 导航栏交互

- 滚动时导航栏自动收窄 / 背景从透明变为毛玻璃
- 当前所在 section 在导航栏高亮（用 Intersection Observer 实现）
- 点击导航项平滑滚动到对应 section
- 滚动超过一屏后显示「回到顶部」按钮，带淡入淡出

### 6.3 其他交互细节

- 所有可点击元素加 hover 反馈（缩放 / 亮度 / 下划线）
- 卡片 hover 时图片轻微放大 + 阴影加深
- 联系按钮点击后弹出联系方式面板（微信二维码 / 邮箱复制 / 链接跳转）
- 页面切换路由过渡动画（如果用了路由）
- 自定义鼠标光标（可选，如果符合整体调性）
- 键盘可访问性：Tab 可聚焦、Enter 可触发、焦点有可见 outline

### 6.4 404 页面

- 设计一个符合整体暗色科技风的 404 页面
- 包含返回首页按钮
- 加一点创意动效（粒子 / 故障效果）

---

&nbsp;

## 第七步 — 移动端适配

把以下指令发给智能体：

---

网站目前是 PC 端 1700px 版心，请帮我做完整的移动端适配。要求：

### 7.1 断点策略

- **桌面端**：≥ 1024px，版心 1700px，保持现有布局
- **平板端**：768px - 1023px，版心收窄至 100%，适当减少 padding
- **移动端**：≤ 767px，单列堆叠布局

### 7.2 具体调整

- **导航栏**：移动端改为汉堡菜单，点击展开全屏抽屉式导航
- **Hero 首屏**：视频背景降级为静态封面图（移动端不加载视频，节省流量）；大标题字号缩小但保持视觉冲击
- **个人经历模块**：头像和介绍从左右布局改为上下堆叠
- **精选项目卡片**：从多列网格改为单列纵向滚动，卡片高度适配移动端
- **个人优势卡片**：从多列改为单列或双列
- **底部联系模块**：按钮改为全宽堆叠
- **项目详情弹窗**：移动端改为全屏页面，关闭按钮固定顶部
- **所有字号**：用 clamp() 实现流式缩放，不要硬切
- **GSAP 动画**：移动端简化或关闭 parallax，保留基本进场动画
- **触摸交互**：确保所有 hover 效果在触摸设备上有对应的 active/tap 反馈

### 7.3 性能

- 移动端不加载 Hero 视频，用 poster 图替代
- 图片用 `<picture>` + srcset 提供移动端小图
- 移动端关闭自定义光标（如果有的话）
- 检查移动端滚动性能，必要时降低动画复杂度

---

&nbsp;

## 第八步 — SEO + 元数据

把以下指令发给智能体：

---

请帮我完善 SEO 和社交分享元数据：

### 8.1 基础 SEO

- 每个页面设置 `<title>` 和 `<meta description>`
- 首页 title 格式：「姓名 — 视觉设计师 / AI设计师 / 品牌设计师」
- 设置 `<html lang="zh-CN">`
- 添加 canonical link
- 生成 `robots.txt`（允许全部抓取）
- 生成 `sitemap.xml`

### 8.2 社交分享（Open Graph + Twitter Card）

- `og:title` / `og:description` / `og:type=website` / `og:url`
- `og:image`：1200×630 封面图（用 Hero 截图或专门设计一张）
- `twitter:card=summary_large_image`
- `twitter:title` / `twitter:description` / `twitter:image`

### 8.3 结构化数据

- 添加 JSON-LD Person schema：姓名、职业、简介、社交链接
- 添加 JSON-LD WebSite schema

### 8.4 Favicon & PWA

- 生成 favicon.ico + apple-touch-icon
- 添加 manifest.json，支持「添加到主屏幕」
- 主题色 meta 与网站主色一致

---

&nbsp;

## 第九步 — 构建 & 部署 ✅ 已完成（2026-07-01）

### 已部署信息

| 项目 | 详情 |
|------|------|
| 线上地址 | `https://max-portfolio-7i0.pages.dev/` |
| GitHub | `https://github.com/youyituomaoxian/max-portfolio` |
| 部署方式 | Cloudflare Pages（免费，全球 CDN） |
| 构建产物 | `dist/` ≈ 2.5MB（JS bundles 507KB + 视频 1.8MB） |

### 架构说明

```
本地                                 线上
┌──────────────────┐              ┌──────────────────┐
│ admin.html       │              │ 作品集首页        │
│ (后台管理，       │   git push   │ xxx.pages.dev    │
│  仅本地使用)      │────────────→│ (纯展示，无后台)  │
│                  │              │                  │
│ 编辑 profile.js  │              │ Cloudflare 自动  │
│ 导出覆盖源文件    │              │ npm run build    │
└──────────────────┘              └──────────────────┘
```

> **后台管理只在本地运行**，不在线上暴露。编辑流程：本地 `npm run dev` → `localhost:5175/admin.html` → 导出 profile.js → `git push` → 1-2 分钟后线上更新。

### 预检清单（部署前已执行）

- [x] 邮箱替换 `youyituomaoxian@163.com`（index.html + profile.js）
- [x] Favicon PNG 5 个尺寸（16/32/180/192/512）
- [x] OG 社交分享图 1200×630 PNG
- [x] Hero 视频压缩 10.5MB → 1.8MB
- [x] `_headers` 安全头 + 缓存策略
- [x] `.gitignore` 完善（排除 node_modules/dist/环境变量/临时文件）
- [x] Vite 构建配置优化（`usePolling` watch、vendor chunk 分离）

### 踩坑记录

| 问题 | 原因 | 解决 |
|------|------|------|
| Framework 误识别 VitePress | Cloudflare 自动检测不准 | 手动选 None，构建命令 `npm run build`，输出 `dist` |
| 首页找不到 Pages | 新版 Cloudflare 把 Pages 藏在 Workers 底部 | 页面底部「想要部署 Pages？立即开始」 |
| `admin.html` 302 跳转 | Cloudflare Clean URLs 去 .html 后缀 | 后台改为仅本地使用，不上线 |
| Admin 数据加载失败 | 生产环境 `src/data/profile.js` 已被 Vite 打包 | 改用 JS 模块 import 直接导入 |

---

## 第十步 — 日常更新

### 方式一：手动更新（你自主操作）

**前置条件**：电脑已安装 Node.js 和 Git。

```
═══════════════════════════════════════════════════════
  每次更新的完整流程（3 步）
═══════════════════════════════════════════════════════

  Step 1 — 启动后台
  ┌─────────────────────────────────────────────────┐
  │ 打开命令行（Win+R → cmd）                         │
  │                                                 │
  │ cd I:\workbuddy\UI\Portfolio_2                  │
  │ npm run dev                                     │
  │                                                 │
  │ → 浏览器打开 http://localhost:5175/admin.html     │
  │ → 编辑各项数据                                    │
  │ → 点「导出 profile.js」覆盖 src/data/profile.js   │
  └─────────────────────────────────────────────────┘

  Step 2 — 构建验证（可选但推荐）
  ┌─────────────────────────────────────────────────┐
  │ npm run build                                   │
  │ npx vite preview                                │
  │                                                 │
  │ → 浏览器打开 http://localhost:4173 验证效果        │
  └─────────────────────────────────────────────────┘

  Step 3 — 推送上线
  ┌─────────────────────────────────────────────────┐
  │ git add -A                                      │
  │ git commit -m "更新内容描述"                      │
  │ git push                                        │
  │                                                 │
  │ → 等待 1-2 分钟 → 刷新网站看效果                   │
  └─────────────────────────────────────────────────┘
═══════════════════════════════════════════════════════
```

**回滚方法**：
- Cloudflare Pages Dashboard → Deployments → 点击旧版本 → Rollback，一键回退。
- 或者 `git log` 找旧 commit → `git revert <commit>` → `git push`。

---

### 方式二：通过 WorkBuddy 更新（告诉 AI 帮你做）

直接跟我说要改什么，我来操作全部流程：

```
你说：
  "帮我把邮箱改成 xxx@xxx.com"
  "新增一个品牌设计项目，叫XX品牌视觉"
  "替换微信二维码图片"
  "把Hero视频换成新的"

我做的事：
  ① 启动开发服务器
  ② 打开后台管理页面
  ③ 你编辑数据 → 导出 profile.js
  ④ 我执行 git add → commit → push
  ⑤ 等 Cloudflare 部署 → 确认上线
```

**两种方式对比**：

| | 手动 | WorkBuddy |
|---|:--:|:--:|
| 需要记命令 | 是 | 否 |
| 需要自己开终端 | 是 | 否 |
| 适合 | 小修改、随时改 | 大改动、改多项 |
| git push | 需要输 Token | 自动处理 |

---

## 流程总览

| 步骤 | 内容 | 状态 |
|------|------|------|
| 第 1 步 | 基础版本搭建（含 ReactBits T1+T2 动画） | ✅ 已完成 |
| — | 对抗审查修复（adversarial review：14 缺陷 → 13 已修复） | ✅ 已完成 |
| 第 2 步 | 参考图调风格 | ✅ 已完成（2026-06-30） |
| 第 3 步 | reactbits 动画集成 | ✅ 已完成（已合并到第 1 步执行） |
| 第 4 步 | GSAP 进场动画（强视觉 opening + parallax） | ✅ 已完成（2026-06-30） |
| 第 5 步 | 性能审查优化 | ✅ 已完成（2026-06-30） |
| 第 6 步 | 项目详情页 & 交互补全 | ✅ 已完成（2026-06-30） |
| 第 7 步 | 移动端适配 | ✅ 已完成（2026-06-30） |
| 第 8 步 | SEO + 元数据 | ✅ 已完成（2026-06-30） |
| 第 9 步 | 构建 & 部署 | ✅ 已完成（2026-07-01）— Cloudflare Pages |
| 第 10 步 | 日常更新流程 | ✅ 已文档化（2026-07-01） |
