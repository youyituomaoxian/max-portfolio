# 2026-06-29 工作日志

## 个人站设计步骤文档完善
- 审查 `个人站设计步骤.md`，发现 13 项缺口
- 根据用户反馈更新文档，新增 4 个步骤：
  - 第 6 步：项目详情页 & 交互补全（含弹窗、导航高亮、回到顶部、404 页面、键盘可访问性）
  - 第 7 步：移动端适配（三级断点、汉堡菜单、视频降级、触摸交互）
  - 第 8 步：SEO + 元数据（Open Graph、JSON-LD、Favicon、PWA manifest）
  - 第 9 步：构建 & 部署（推荐 Cloudflare Pages 零成本方案，备选 Vercel）
- 用户决策：内容资产后期替换、设计系统由 AI 自行决定

## 第 1 步：基础版本搭建
- 技术栈：React 18 + Vite 5 + Tailwind CSS 3
- 设计系统：暗色科技风，品牌蓝 #005EAE，字体 Space Grotesk + Inter + Noto Sans SC
- 实现 5 个完整模块：Navbar / Hero / About / Projects / Expertise / Contact
- Hero：全屏首页，标题 hover 字符变色，mousemove 光晕跟随，视频背景占位
- About：左右布局，头像占位 + 4 项数据卡片 + 时间线经历 + 教育背景
- Projects：2 列大卡片网格，hover 效果，分类徽章
- Expertise：3 列能力卡片 + 6 列工具技能
- Contact：3 列联系方式 + 版权信息
- 全局样式：自定义滚动条、毛玻璃面板、渐变标题、卡片动效
- 数据源：`src/data/profile.js`，基于用户简历内容填充

## DotField 互动粒子背景
- 实现 Canvas 版 DotField 组件 (`src/components/DotField.jsx`)
- 网格圆点 + 光标膨胀排斥 (bulgeOnly) + 径向渐变 + 光晕
- 已集成到 Hero 首屏，替换原静态渐变背景
- 参数：dotSpacing=14, cursorRadius=500, bulgeStrength=67, glowRadius=160
- 配色适配品牌蓝 #005EAE

## 品牌色切换：蓝色 → 紫色
- 用户指令撤回 DotField 背景集成，品牌色全面切换为紫色
- 主色：`#A855F7`，浅色：`#C084FC`，辉光：`rgba(168,85,247,0.35)`
- 更新范围：tailwind.config.js、index.css、全部 6 个组件、DotField 默认值

## ReactBits 动画组件集成 (T1 + T2)
- 安装依赖：gsap + @gsap/react + motion
- 从 GitHub (DavidHDev/react-bits) 获取 6 个官方组件源码：
  - T1: SplitText (gsap/ScrollTrigger), CountUp (motion/useSpring), AnimatedContent (gsap/ScrollTrigger)
  - T2: GradientText (motion/useAnimationFrame), SpotlightCard (纯 CSS 径向渐变), TiltedCard (motion, 已就绪未集成)
- 所有组件放在 `src/components/reactbits/`
- 集成情况：
  - Hero: SplitText 逐字入场替换 hover 字符变色 + GradientText 流动渐变替换静态 text-gradient
  - About: CountUp 数字滚动替换静态 stats (stagger delay 0.2s/项)
  - Projects/Expertise: SpotlightCard 紫色光晕跟随包裹卡片
  - App.jsx: AnimatedContent 包裹 4 section (80px 上浮 + 淡入, threshold=0.15)
  - TiltedCard: 代码就绪，待后期图片到位再集成
- Profile 数据新增 numericValue/suffix 字段支持 CountUp
