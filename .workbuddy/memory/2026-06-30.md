# 2026-06-30 工作日志

## 对抗审查 #1（主项目）
- 14 缺陷 → 13 修复 + 1 暂缓（CountUp DOM 操作）
- 主要修复：ErrorBoundary(6 section)、IntersectionObserver 滚动高亮、Canvas 批处理、SplitText 守卫、列表 key 稳定化
- 新增 `src/utils/scrollTo.js`、`src/components/ErrorBoundary.jsx`

## 对抗审查 #2
- 14 缺陷 → 12 修复 + 1 驳回 + 1 暂缓
- P0: GSAP ScrollTrigger 全局清理 / body scroll 引用计数锁 / img onError
- 新增 `src/utils/bodyScrollLock.js`
- 审查报告：`.workbuddy/review/00_preflight.md` ~ `05_final.md`

## 项目模块改进
- **ProjectModal**: 说明+标签移至顶部、视频支持(横版对齐/竖版居中+模糊背景)、GalleryItem+MediaElement 子组件
- **ImageReveal**: 修复 clipPath 方向(遮罩覆盖→向上撤走)
- **回到顶部按钮**: 右下角固定，scrollY>60 淡入淡出(Step 6.2.4)
- **404 页面**: `public/404.html` 纯静态零依赖(Step 6.4)

## 风格调整（参考图 5416231531）
- 品牌色：紫色#A855F7 → 橙色#FF6B00
- Hero 视频背景：`public/videos/hero-bg.mp4`，autoPlay+muted+loop
- Hero 动画：scale 2.5→1.5、blur 30→20px、easing expo.out

## 性能优化（Step 5）
- 字体异步加载、裁剪未用字重、React vendor chunk 分离、Hero 视频 preload=metadata
- 待手动：压缩 hero-bg.mp4(11→2-3MB)、替换占位图为 WebP

## 移动端适配（Step 7）
- 汉堡菜单→全屏 drawer、视频移动端隐藏、GSAP parallax 宽度守卫、tap:active 反馈

## SEO + 元数据（Step 8）
- OG/Twitter Card/JSON-LD/robots/sitemap/manifest/favicon

## 管理面板
- **初始化**：完整 7 tab 编辑器 + 导入/导出 + 预览(后移除)
- **解析器修复**：`normalizeJSToJSON` 重写(引号属性名、单双引号、中文引号转义、尾逗号、注释剥离)
- **对抗审查 #3**: 8 缺陷 → 6 修复(csp/eval、fetch 路径、字段丢弃、revokeObjectURL 时序、转义处理、重复 key)
- **交互优化**: TagsInput 组件(4 处替换)、项目分类管理、预览固定/桌面缩放/实时刷新、新项目置顶
- 最终采纳第一性原理，移除预览面板，纯粹后台管理

## 交互补全（Step 6）
- 联系面板：邮箱复制+Toast、微信/公众号二维码弹窗
- 数据源改造：`profile.js` 增加 `_previewData` sessionStorage 检测(支持预览覆盖)
